function calcularPotencia (base, expoente)
{
    console.log(`Base: ${base}`);
    console.log(`Expoente: ${expoente}`);

    let resultado = Math.pow(base, expoente);

    console.log(`Resultado: ${resultado}\n`);
};

calcularPotencia(3, 3);
calcularPotencia(7, 3);